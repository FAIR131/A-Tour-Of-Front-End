1.运算符的优先级?
    - ()
    - ++ -- !
    - * / %
    - + - 
    - >= < 
    - === !===
    - && 
    - ||
    - , .

2.三元运算符的作用和语法? x
    - 自定义bool值的返回
    - 判断条件 ? true : false;

3.a++和++a的区别?
    - 运行顺序不同
    - ++a , 先计算,直接覆盖原来的变量
    - a++ ,保持原来的结果，后计算再覆盖

4.输入框prompt,输入的数据类型?
    - String
